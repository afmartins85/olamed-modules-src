CMAKE_MINIMUM_REQUIRED( VERSION 2.8 )

SET(PROJECT_TOP_DIR ${CMAKE_CURRENT_SOURCE_DIR})
SET(COMMON_DIR ${PROJECT_TOP_DIR})

SET(COMMON_BASE_SRC
        ${COMMON_DIR}/healthsensors.h
        ${COMMON_DIR}/healthsensors.cpp
        PARENT_SCOPE
	)
ADD_DEFINITIONS( -std=c++11 )
